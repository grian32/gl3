// e2e: {"mode":"run","exit":42}
import "../support/padding.gl3"
import "../support/recursive_types.gl3"
fnc main() -> int32 {
    def Node tail = Node:{42i32, 0 as Node*}
    def Node head = Node:{7i32, &tail}
    if pass_node(&head) != &head { return 1i32 }
    return read_next(&head)
}
